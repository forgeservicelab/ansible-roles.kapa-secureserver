---
xroad_superuser: "grandmaster"
xroad_admin_fqdn: "/CN={{ ansible_fqdn }}"
xroad_admin_dn_s: "IP:{{ ansible_default_ipv4.address }},DNS:{{ ansible_hostname }}"
xroad_service_fqdn: "/CN={{ ansible_fqdn }}"
xroad_service_dn_s: "IP:{{ ansible_default_ipv4.address }},DNS:{{ ansible_hostname }}"
